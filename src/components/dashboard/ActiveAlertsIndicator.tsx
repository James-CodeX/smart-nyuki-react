import React from 'react';

// This component is now disabled as we're using the AlertIndicator component
// for a more subtle notification in the navigation.

const ActiveAlertsIndicator: React.FC = () => {
  // Return null to not show anything - red dot indicators are used instead
  return null;
};

export default ActiveAlertsIndicator; 